# Projects Page 設計文件

> 建立日期：2026-01-30

## 概述

以專案為單位的詳細資訊頁面，包含資料來源、commit tree、開發過程摘要，並支援用戶新增專案目標描述以輔助 AI 生成更精準的工作摘要。

## 需求摘要

| 項目 | 決定 |
|------|------|
| 專案目標描述 | AI 背景資訊 + 用戶筆記 |
| 時間軸粒度 | 可選：天/週/月/季/年 |
| 動態載入 | 時間範圍選擇器 + 無限滾動 |
| 資料來源 | 混合顯示 + checkbox 篩選 |
| Commit 顯示 | 互動式：點擊展開詳情 |
| 開發摘要 | 快取 + 手動重新生成 |
| 頁面佈局 | 左右分欄 master-detail |
| 目標描述編輯 | 結構化表單 |

---

## 頁面結構

```
┌─────────────────────────────────────────────────────────────────┐
│  Projects                                            [+ 新增專案] │
├──────────────────────┬──────────────────────────────────────────┤
│                      │                                          │
│  🔍 搜尋專案...       │  📁 Recap                                │
│                      │  ──────────────────────────────────────  │
│  ┌────────────────┐  │                                          │
│  │ 📁 Recap      │◀─│  [專案資訊] [時間軸] [設定]               │
│  │ 12.5h · 3來源  │  │                                          │
│  └────────────────┘  │  ┌─ 專案資訊 ─────────────────────────┐  │
│  ┌────────────────┐  │  │ 目標：工作追蹤桌面應用              │  │
│  │ 📁 API Server │  │  │ 技術：Tauri, React, Rust            │  │
│  │ 8.2h · 2來源   │  │  │ 狀態：開發中                        │  │
│  └────────────────┘  │  └────────────────────────────────────┘  │
│  ┌────────────────┐  │                                          │
│  │ 📁 ML Pipeline│  │  ┌─ 本週摘要 ──────────────────────────┐  │
│  │ 5.0h · 1來源   │  │  │ 完成專案頁面設計...    [🔄 重新生成] │  │
│  └────────────────┘  │  └────────────────────────────────────┘  │
│                      │                                          │
│  顯示隱藏專案 ▢      │  ┌─ 統計 ─────────────────────────────┐  │
│                      │  │ 總時數: 12.5h │ 來源: 3 │ 活躍: 30d │  │
│                      │  └────────────────────────────────────┘  │
├──────────────────────┴──────────────────────────────────────────┤
│  左側 (250px)        │  右側 (彈性寬度)                          │
└─────────────────────────────────────────────────────────────────┘
```

### 左側面板（專案列表）
- 搜尋框：依專案名稱過濾
- 專案卡片：顯示名稱、總時數、來源數量
- 選中狀態：高亮顯示當前專案
- 底部：顯示/隱藏已隱藏專案的 checkbox

### 右側面板（專案詳情）
- 標題列：專案名稱 + Tab 切換
- 三個 Tab：專案資訊、時間軸、設定

---

## 專案資訊 Tab

```
┌─ 專案資訊 Tab ──────────────────────────────────────────────────┐
│                                                                  │
│  ┌─ 專案描述 ─────────────────────────────────── [✏️ 編輯] ──┐  │
│  │                                                            │  │
│  │  📎 專案目標                                               │  │
│  │  建立工作追蹤桌面應用，自動收集開發活動並生成工作報告       │  │
│  │                                                            │  │
│  │  🛠️ 技術棧                                                 │  │
│  │  Tauri v2, React 18, TypeScript, Rust, SQLite             │  │
│  │                                                            │  │
│  │  ⭐ 關鍵功能                                               │  │
│  │  • 自動捕獲 Claude Code / Antigravity sessions            │  │
│  │  • Git commit 追蹤與分析                                   │  │
│  │  • 工作摘要生成與 Tempo 同步                               │  │
│  │                                                            │  │
│  │  📝 備註                                                   │  │
│  │  Q1 目標：完成專案頁面與效能優化                           │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─ 開發摘要 ────────────────────────────────── [🔄 重新生成] ─┐ │
│  │                                                            │  │
│  │  📅 本週 (1/27 - 1/30)                     ⚡ 有新活動     │  │
│  │  ────────────────────────────────────────────────────────  │  │
│  │  完成 Dashboard 頁面重構，將原本 655 行的元件拆分為 10     │  │
│  │  個子模組。新增 WorkItems 頁面的 Timeline 視圖，支援       │  │
│  │  小時級別的工作紀錄顯示。                                  │  │
│  │                                                            │  │
│  │  📅 上週 (1/20 - 1/26)                                     │  │
│  │  ────────────────────────────────────────────────────────  │  │
│  │  重構 Rust commands 層，將 work_items.rs 從 2295 行       │  │
│  │  拆分為 6 個檔案...                        [展開更多 ▼]   │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─ 資料來源 ────────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  ✅ Claude Code    45 sessions    8.2h    最近: 2小時前   │  │
│  │  ✅ Antigravity    12 sessions    3.1h    最近: 昨天      │  │
│  │  ✅ Git            128 commits    -       最近: 30分鐘前   │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 專案描述區塊
- 結構化欄位：目標、技術棧、關鍵功能、備註
- 點擊編輯按鈕開啟 Modal 編輯
- 這些資訊會作為 AI 生成摘要的背景 context

### 開發摘要區塊
- 按週顯示快取的摘要
- 「有新活動」標示提醒用戶可重新生成
- 點擊重新生成會呼叫 LLM 更新摘要

### 資料來源區塊
- 顯示各來源的統計：session 數、時數、最近活動時間

---

## 時間軸 Tab

```
┌─ 時間軸 Tab ────────────────────────────────────────────────────┐
│                                                                  │
│  ┌─ 控制列 ──────────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  時間單位: [日 ▼]     範圍: [2025 Q1 ▼]     🔍 搜尋...    │  │
│  │                                                            │  │
│  │  來源: ☑️ Claude Code  ☑️ Antigravity  ☑️ Git             │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─ 時間軸內容 ──────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  📅 2025-01-30 (四)                          總計: 3.2h   │  │
│  │  ├─────────────────────────────────────────────────────── │  │
│  │  │  🤖 Claude Code Session              14:30 - 17:45     │  │
│  │  │  「實作專案頁面設計」                                   │  │
│  │  │  ├── 📝 feat: Add Projects page layout    15:23       │  │
│  │  │  ├── 📝 feat: Add ProjectList component   16:01       │  │
│  │  │  └── 📝 fix: Fix timeline scroll issue    17:30       │  │
│  │  │                                                        │  │
│  │  │  🌀 Antigravity Session              10:00 - 11:30     │  │
│  │  │  「Debug API response parsing」                        │  │
│  │  │  └── 📝 fix: Handle null response        11:15        │  │
│  │  │                                                        │  │
│  │  ├─────────────────────────────────────────────────────── │  │
│  │                                                            │  │
│  │  📅 2025-01-29 (三)                          總計: 5.1h   │  │
│  │  ├─────────────────────────────────────────────────────── │  │
│  │  │  🤖 Claude Code Session              09:00 - 14:00     │  │
│  │  │  「重構 Dashboard 元件」                                │  │
│  │  │  ├── 📝 refactor: Split Dashboard...      10:45       │  │
│  │  │  ├── 📝 refactor: Extract StatsCard...    12:30       │  │
│  │  │  └── [+3 more commits]                                 │  │
│  │  │                                                        │  │
│  │  ├─────────────────────────────────────────────────────── │  │
│  │                                                            │  │
│  │  ⏳ 載入更多...                                            │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### 控制列
- 時間單位選擇：日/週/月/季/年
- 時間範圍選擇：快速跳轉到特定期間
- 搜尋框：過濾 commit message 或 session 標題
- 來源篩選：checkbox 勾選要顯示的來源

### 時間軸內容
- 依選擇的時間單位分組
- 每組顯示：日期、星期、該時段總時數
- Session 顯示：來源 icon、標題、時間範圍
- Commits 巢狀在 session 下方，顯示 message 和時間
- 超過 3 個 commits 摺疊，點擊展開

### 無限滾動
- 滾到底部自動載入該時間範圍內的更多資料
- 載入時顯示 spinner

---

## Commit 詳情展開

```
┌─ Commit 詳情（點擊展開後）─────────────────────────────────────┐
│                                                                  │
│  📝 feat: Add Projects page layout                              │
│  ────────────────────────────────────────────────────────────── │
│                                                                  │
│  🔖 a1b2c3d4                          👤 weifan · 15:23         │
│                                                                  │
│  ┌─ 檔案變更 ──────────────────────── +156 / -23 ─────────────┐ │
│  │                                                            │  │
│  │  📄 web/src/pages/Projects/index.tsx          +89  -0     │  │
│  │  📄 web/src/pages/Projects/components/...     +45  -12    │  │
│  │  📄 web/src/services/projects.ts              +22  -11    │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  [查看 Diff ▶]                                    [收合 ▲]      │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 互動行為
- 預設收合，只顯示 commit message 一行
- 點擊展開顯示檔案變更
- 再點「查看 Diff」顯示完整 diff
- 可隨時收合回簡潔模式

---

## 設定 Tab

```
┌─ 設定 Tab ──────────────────────────────────────────────────────┐
│                                                                  │
│  ┌─ 專案設定 ────────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  顯示名稱                                                  │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ Recap 工作追蹤                                        │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  專案路徑                                                  │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ /Users/weifanliao/PycharmProjects/recap              │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  可見性                                                    │  │
│  │  ☑️ 在專案列表中顯示此專案                                 │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌─ 危險區域 ────────────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  🗑️ 移除專案                                               │  │
│  │  從 Recap 中移除此專案（不會刪除實際檔案）                  │  │
│  │                                              [移除專案]    │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 編輯專案描述 Modal

```
┌─ 編輯專案描述 Modal ────────────────────────────────────────────┐
│                                                           [✕]   │
│  ────────────────────────────────────────────────────────────── │
│                                                                  │
│  專案目標 *                                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 建立工作追蹤桌面應用，自動收集開發活動並生成工作報告      │   │
│  └──────────────────────────────────────────────────────────┘   │
│  💡 描述專案要解決的問題或達成的目標                             │
│                                                                  │
│  技術棧                                                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Tauri v2, React 18, TypeScript, Rust, SQLite            │   │
│  └──────────────────────────────────────────────────────────┘   │
│  💡 使用的程式語言、框架、資料庫等                               │
│                                                                  │
│  關鍵功能                                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ • 自動捕獲 Claude Code / Antigravity sessions           │   │
│  │ • Git commit 追蹤與分析                                  │   │
│  │ • 工作摘要生成與 Tempo 同步                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│  💡 列出主要功能，每行一項                                       │
│                                                                  │
│  備註                                                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Q1 目標：完成專案頁面與效能優化                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│  💡 其他補充資訊、開發計畫、注意事項等                           │
│                                                                  │
│  ────────────────────────────────────────────────────────────── │
│                                        [取消]     [儲存]        │
└──────────────────────────────────────────────────────────────────┘
```

### 結構化欄位
- 專案目標：必填，作為 AI 摘要的核心 context
- 技術棧：選填，幫助 AI 理解技術細節
- 關鍵功能：選填，多行輸入
- 備註：選填，自由文字

---

## 資料模型

### 新增資料表：project_descriptions

```sql
CREATE TABLE project_descriptions (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    project_name TEXT NOT NULL,
    goal TEXT,                     -- 專案目標
    tech_stack TEXT,               -- 技術棧
    key_features TEXT,             -- 關鍵功能（JSON array）
    notes TEXT,                    -- 備註
    orphaned BOOLEAN DEFAULT 0,    -- 專案已無 work_items
    orphaned_at DATETIME,          -- 標記時間
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, project_name)
);
```

### 新增資料表：project_summaries

```sql
CREATE TABLE project_summaries (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    project_name TEXT NOT NULL,
    period_type TEXT NOT NULL,     -- 'week' | 'month'
    period_start DATE NOT NULL,    -- 週/月的起始日
    period_end DATE NOT NULL,
    summary TEXT NOT NULL,         -- AI 生成的摘要
    data_hash TEXT,                -- 用於偵測資料變更
    orphaned BOOLEAN DEFAULT 0,
    orphaned_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, project_name, period_type, period_start)
);
```

---

## 生命週期管理

### 資料保留策略

| 資料類型 | 保留策略 |
|----------|----------|
| project_descriptions | 永久保留，orphaned 標記僅用於 UI 分類 |
| project_summaries | 永久保留，作為專案歷史紀錄 |
| work_items | 依現有邏輯（用戶控制） |

### 生命週期狀態

| 情境 | 處理方式 |
|------|----------|
| Work items 被刪除 | 描述/摘要保留，標記 orphaned |
| 手動專案被移除 | 用戶選擇：隱藏 or 完全刪除 |
| 專案重新出現活動 | 自動清除 orphaned 標記 |

---

## API Commands

```rust
// 專案描述 CRUD
get_project_description(project_name) -> ProjectDescription
update_project_description(project_name, data) -> Result

// 專案摘要
get_project_summaries(project_name) -> Vec<ProjectSummary>
generate_project_summary(project_name, period) -> Summary
check_summary_freshness(project_name) -> FreshnessStatus

// 時間軸資料（支援分頁）
get_project_timeline(
    project_name,
    time_unit: 'day' | 'week' | 'month' | 'quarter' | 'year',
    range_start: Date,
    range_end: Date,
    sources: Vec<String>,          -- 篩選來源
    cursor: Option<String>,        -- 無限滾動游標
    limit: i32                     -- 每次載入數量
) -> TimelineResponse

// Commit Diff
get_commit_diff(project_name, commit_hash) -> DiffResponse
```

---

## TypeScript 類型定義

```typescript
interface ProjectDescription {
  project_name: string
  goal: string | null
  tech_stack: string | null
  key_features: string[] | null
  notes: string | null
}

interface ProjectSummary {
  period_type: 'week' | 'month'
  period_start: string
  period_end: string
  summary: string
  is_stale: boolean              -- 有新資料待更新
}

interface TimelineGroup {
  period_label: string           -- "2025-01-30" 或 "Week 5"
  period_start: string
  period_end: string
  total_hours: number
  sessions: TimelineSession[]
  standalone_commits: TimelineCommit[]  -- 不屬於 session 的
}

interface TimelineResponse {
  groups: TimelineGroup[]
  next_cursor: string | null     -- 無限滾動用
  has_more: boolean
}
```

---

## 檔案結構

### 前端

```
web/src/
├── pages/Projects/
│   ├── index.tsx                    # 主頁面 (~150 行)
│   ├── components/
│   │   ├── ProjectList/
│   │   │   ├── index.tsx            # 左側專案列表
│   │   │   ├── ProjectCard.tsx      # 單一專案卡片
│   │   │   └── ProjectSearch.tsx    # 搜尋框
│   │   ├── ProjectDetail/
│   │   │   ├── index.tsx            # 右側詳情容器
│   │   │   ├── InfoTab.tsx          # 專案資訊 Tab
│   │   │   ├── TimelineTab.tsx      # 時間軸 Tab
│   │   │   └── SettingsTab.tsx      # 設定 Tab
│   │   ├── Timeline/
│   │   │   ├── TimelineControls.tsx # 控制列
│   │   │   ├── TimelineGroup.tsx    # 時間分組
│   │   │   ├── SessionItem.tsx      # Session 項目
│   │   │   ├── CommitItem.tsx       # Commit 項目
│   │   │   └── CommitDiff.tsx       # Diff 視圖
│   │   ├── Summary/
│   │   │   ├── SummaryCard.tsx      # 摘要卡片
│   │   │   └── SummaryGenerator.tsx # 生成中狀態
│   │   └── Modals/
│   │       ├── EditDescriptionModal.tsx  # 編輯專案描述
│   │       └── RemoveProjectModal.tsx    # 移除確認
│   └── hooks/
│       ├── useProjects.ts           # 專案列表 hook
│       ├── useProjectDetail.ts      # 專案詳情 hook
│       ├── useProjectTimeline.ts    # 時間軸 + 無限滾動
│       └── useProjectSummary.ts     # 摘要管理 hook
│
├── services/projects.ts             # 新增 API 函數
└── types/projects.ts                # 新增類型定義
```

### 後端

```
web/src-tauri/src/
├── commands/projects/
│   ├── mod.rs                       # 匯出所有 commands
│   ├── types.rs                     # 新增類型 (修改)
│   ├── queries.rs                   # 現有查詢 (修改)
│   ├── descriptions.rs              # 新增：專案描述 CRUD
│   ├── summaries.rs                 # 新增：摘要管理
│   ├── timeline.rs                  # 新增：時間軸查詢
│   └── diff.rs                      # 新增：Git diff
│
├── db/
│   └── migrations/
│       └── add_project_descriptions.sql  # 新增資料表
│
└── services/
    └── summary_generator.rs         # LLM 摘要生成邏輯
```

---

## 現有 API 盤點

### ✅ 已實作（可直接使用）

| 功能 | API | 說明 |
|------|-----|------|
| 專案列表 | `list_projects` | 自動從 work_items 發現專案 |
| 專案詳情 | `get_project_detail` | 含來源統計、最近項目、基本 stats |
| 專案可見性 | `set_project_visibility` | 顯示/隱藏專案 |
| 隱藏專案列表 | `get_hidden_projects` | 取得已隱藏的專案名稱 |
| 專案目錄 | `get_project_directories` | Claude Code 目錄 + Git repo 路徑 |
| 手動新增專案 | `add_manual_project` | 手動加入非自動發現的專案 |
| 移除專案 | `remove_manual_project` | 移除手動新增的專案 |
| 單日時間軸 | `get_timeline` | **單日**的 sessions + commits |
| LLM 摘要生成 | `LlmService::summarize_project_work` | 已有專案工作摘要功能 |
| LLM 週期摘要 | `LlmService::summarize_work_period` | 支援 weekly/monthly 摘要 |

### ⚠️ 部分實作（需擴充）

| 功能 | 現況 | 需要擴充 |
|------|------|----------|
| 時間軸查詢 | 只支援**單日** | 需支援多時間單位（週/月/季/年）+ cursor 分頁 |
| 專案 stats | 基本統計 | 需加入來源分別統計、活躍天數等 |

### ❌ 尚未實作（需新增）

| 功能 | 說明 | 複雜度 |
|------|------|--------|
| **專案描述 CRUD** | `project_descriptions` 資料表 + API | 中 |
| **專案摘要快取** | `project_summaries` 資料表 + API | 中 |
| **摘要新鮮度檢查** | 計算 data_hash 偵測變更 | 低 |
| **多時間單位時間軸** | 依日/週/月/季/年分組 + 無限滾動 | 高 |
| **Git Diff 查詢** | 呼叫本地 git show 取得 diff | 中 |
| **Commit 檔案變更** | 取得 commit 的 files changed 詳情 | 中 |

### 資料表狀態

| 資料表 | 狀態 | 說明 |
|--------|------|------|
| `project_preferences` | ✅ 已存在 | 儲存可見性、display_name、手動專案 |
| `project_descriptions` | ❌ 需新增 | 專案目標、技術棧、關鍵功能、備註 |
| `project_summaries` | ❌ 需新增 | AI 生成的週/月摘要快取 |

---

## 實作順序

### Phase 1: 資料層（後端）
1. 新增 `project_descriptions` 資料表（在現有 init 中加入）
2. 新增 `project_summaries` 資料表
3. 新增 `descriptions.rs` - 專案描述 CRUD commands
4. 新增 `summaries.rs` - 摘要管理 commands（整合現有 LlmService）
5. 擴充 `types.rs` - 新增 ProjectDescription, ProjectSummary 等類型

### Phase 2: 前端基礎
6. 擴充 `types/projects.ts` - 新增 TypeScript 類型
7. 擴充 `services/projects.ts` - 新增 API 函數
8. 建立 `pages/Projects/` 目錄結構
9. 實作主頁面骨架 `index.tsx`（左右分欄）

### Phase 3: 專案列表與詳情
10. 實作 `ProjectList/` 元件（使用現有 `list_projects` API）
11. 實作 `ProjectDetail/index.tsx` - Tab 容器
12. 實作 `InfoTab.tsx` - 專案資訊（使用現有 `get_project_detail`）
13. 實作 `EditDescriptionModal.tsx` - 編輯專案描述
14. 實作 `SettingsTab.tsx` - 設定（使用現有可見性 API）

### Phase 4: 時間軸
15. 新增 `timeline.rs` - 多時間單位時間軸查詢
16. 新增 `diff.rs` - Git diff 查詢
17. 實作 `TimelineTab.tsx` + `TimelineControls.tsx`
18. 實作 `TimelineGroup.tsx` + `SessionItem.tsx`
19. 實作 `CommitItem.tsx` + `CommitDiff.tsx`
20. 實作 `useProjectTimeline.ts` - 無限滾動 hook

### Phase 5: 摘要功能
21. 實作 `SummaryCard.tsx` - 摘要顯示
22. 實作 `useProjectSummary.ts` - 摘要管理 hook
23. 整合 LLM 摘要生成（使用現有 LlmService）
24. 實作新鮮度檢查 UI
